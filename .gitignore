.DS_Store
# local secrets / env files (never commit)
.env
cronova.key
cronova.env
*.db
*.db-wal
*.db-shm
*.db-journal
# built binaries at repo root (anchored so they don't match cmd/cronova* source dirs)
/cronova
/cronova.exe
/cronova.exe~
/cronova-executor
/cronova-executor.exe
/cronova-executor.exe~
# release artifacts (make release)
/dist/
/data/
/logs/
# local workspace / runtime working directory (never commit)
/workspaces/
.workspaces/
# DAG-generated build artifacts (JARs, logs, reports — never commit)
/artifacts/
# local Maven cache (never commit)
.m2/
# Maven build output inside templates (never commit)
templates/**/target/
# scratch screenshots / working artifacts (never commit)
/tmp/
.tmp/
node_modules/
# mkdocs build output + docs venv
/site/
/.venv-docs/
/.venv/
# local publishing/marketing playbook — CONTAINS SECRETS (dev.to API key). Never commit.
/publish/
# graphify knowledge-graph artifacts (generated locally, not shipped)
/graphify-out/
